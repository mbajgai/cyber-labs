<!doctype html>
<html>
<head><title><%= product.name || "Unknown" %></title></head>
<body>
  <a href="/">Back</a>
  <h1><%= product.name || "Product not found" %></h1>
  <p><%= product.description || "" %></p>
  <p>Price: £<%= product.price || "0.00" %></p>
</body>
</html>
