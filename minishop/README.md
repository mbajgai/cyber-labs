# MiniShop (intentionally vulnerable)

**Run only in an isolated VM / host-only network. Do NOT expose to the internet.**

## Quick start
- `docker compose up --build -d`
- `docker compose exec web node seed.js`
- Visit `http://localhost:3000`
